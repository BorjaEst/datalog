{erl_opts, []}.
{deps, [
    {jiffy, {git, "git://github.com/aboroska/jiffy.git", {branch, "rebar3"}}}
]}.

%% == Shell ==
%% apps to auto-boot with `rebar3 shell'; defaults to apps
%% specified in a `relx' tuple, if any.
{shell, [
    {apps,   [datalog]}
]}.
